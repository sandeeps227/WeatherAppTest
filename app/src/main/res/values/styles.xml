<resources>
    <!-- Base application theme -->
    <style name="Theme.WeatherAppJC" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryDark">@color/purple_700</item>
        <item name="colorAccent">@color/teal_200</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:windowNoTitle">true</item>  <!-- Disable default ActionBar -->
        <item name="android:windowActionBar">false</item> <!-- Disable ActionBar -->
    </style>
</resources>
